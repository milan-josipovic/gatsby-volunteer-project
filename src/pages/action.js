import React, { Component } from "react"
import Layout from "../components/Layout"

export default class Action extends Component {
  render() {
    return <Layout>hello from action page</Layout>
  }
}
